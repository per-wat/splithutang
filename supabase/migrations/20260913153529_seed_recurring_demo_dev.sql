-- Migration-history marker for the visual fixture applied directly to
-- SplitHutangDev. Deliberately contains no DML so production and other
-- environments can never receive the demo rows.

select 1;
